<!DOCTYPE html>
<html>
  <head>
    <title>Admin - Exhibition Platform</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <%= stylesheet_link_tag "application" %>
    <%= javascript_importmap_tags %>
    
    <style>
      .navbar { background-color: rgba(255, 127, 0, 0.9) !important; }
      .btn-primary { background-color: rgba(255, 127, 0, 0.8); border-color: #FF6600; }
      .btn-primary:hover { background-color: rgba(255, 102, 0, 0.9); border-color: #FF5500; }
      .admin-form { background-color: rgba(255, 255, 255, 0.9); padding: 20px; border-radius: 10px; margin-bottom: 20px; }
    </style>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-dark mb-4">
      <div class="container">
        <%= link_to 'Admin Dashboard', admin_exhibitions_path, class: 'navbar-brand' %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#adminNav">
          <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="adminNav">
          <ul class="navbar-nav">
            <li class="nav-item">
              <%= link_to 'Exhibition Centers', admin_exhibition_centers_path, class: 'nav-link' %>
            </li>
            <li class="nav-item">
              <%= link_to 'Exhibitions', admin_exhibitions_path, class: 'nav-link' %>
            </li>
            <li class="nav-item">
              <%= link_to 'Exhibition Types', admin_exhibition_types_path, class: 'nav-link' %>
            </li>
            <li class="nav-item">
              <%= link_to 'View Site', root_path, class: 'nav-link' %>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <div class="container">
      <% if notice %>
        <div class="alert alert-success"><%= notice %></div>
      <% end %>
      
      <div class="admin-dashboard">
        <%= yield %>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
